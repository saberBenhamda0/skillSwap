bind = "0.0.0.0:8000"  # Bind to all network interfaces
workers = 3  # Minimal number of worker processes
accesslog = "-"  # Log access requests to stdout
errorlog = "-"   # Log errors to stdout
loglevel = "info"  # Log level
